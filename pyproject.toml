[project]
name = "antenna-paper-extraction"
version = "0.1.0"
description = "Evidence-grounded extraction of antenna architecture and reported results from scientific papers."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=3.6.0,<4",
    "pillow",
    "pydantic>=2.13.4",
    "pypdfium2>=5.13.0",
    "python-dotenv>=1.2.3",
    "tzdata>=2026.3",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13"]
build-backend = "uv_build"

[project.scripts]
antenna-extract = "antenna_paper_extraction.cli:main"

[dependency-groups]
dev = [
    "pypdf>=6.16.2",
    "pytest>=9.1.1",
    "ruff>=0.16.4",
]
